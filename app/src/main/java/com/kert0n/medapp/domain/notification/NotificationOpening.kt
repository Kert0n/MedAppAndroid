package com.kert0n.medapp.domain.notification

/**
 * С чем человек открыл приложение из шторки: цель — всегда, действие — если он нажал кнопку, а не
 * карточку (PLAN H3 «Уведомления на экране»).
 *
 * Пара, а не два значения порознь: действие без цели бессмысленно — «принял» **что**? — и
 * разъехаться им нельзя. Живёт в домене, потому что о ней говорят и оболочка, и платформа:
 * `extras` — лишь способ довезти её через систему.
 */
data class NotificationOpening(val target: NotificationTarget, val action: NotificationAction?)
