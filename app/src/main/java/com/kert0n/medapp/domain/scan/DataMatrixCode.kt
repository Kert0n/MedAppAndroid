package com.kert0n.medapp.domain.scan

/**
 * DataMatrix реестра маркировки — текст, который отдал сканер (PLAN H5). Код **не разбирается и не
 * нормализуется**: он несёт крипто-хвост, расшифровывать его нечем, а о циклах обновления чужого
 * API мы не знаем ничего — поэтому разделители GS (`U+001D`) между полями `91`/`92` остаются на
 * месте, и любая «чистка» срезала бы значащий символ. Как код уходит на провод — с каким
 * префиксом и в каком поле, — знает сетевая граница (`network/marking`), а не величина (C1).
 */
data class DataMatrixCode(val text: String) {

    init {
        require(text.isNotEmpty()) { "код с коробки не бывает пустым" }
    }
}
