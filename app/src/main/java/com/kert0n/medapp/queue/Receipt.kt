package com.kert0n.medapp.queue

/**
 * Ответ реестра на поручение, как его принёс курьер. Записывается до разбора: полученное
 * подтверждение не теряется, даже если применить его сейчас нечем (PLAN E3). Что в нём, читает
 * только курьер ([Courier.Pass.read]); очередь его носит, хранение раскладывает по колонкам.
 */
data class Receipt(val status: Int, val body: String)
