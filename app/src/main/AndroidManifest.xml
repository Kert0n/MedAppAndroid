<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Отказ камеры ведёт к ручному вводу (H5), поэтому устройство без камеры приложение ставит. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".app.MedApp"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="33"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/full_backup_content"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MedApp">
        <!--
            Клавиатуру окно отдаёт форме, а не двигает собой. Без объявления система выбирает
            режим сама и панорамирует окно: форма поднимает поля своим imePadding, а окно сверх
            того уезжает вверх — верхняя панель прячется под строку состояния, а снизу остаётся
            пустой прямоугольник тем больше, чем ниже было поле (находка владельца 2026-09-17).
            adjustResize отдаёт высоту клавиатуры insets'ами, и подъём остаётся один.
        -->
        <activity
            android:name=".app.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MedApp"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Будильник: сработал — владелец доставки читает обязательства и говорит наступившее (PLAN D8). -->
        <receiver
            android:name=".platform.notifications.ReminderWakeReceiver"
            android:exported="false" />
        <!-- «Принял», «Пропустить», «Отложить» с уведомления — тем же путём, что экран (PLAN D8). -->
        <receiver
            android:name=".platform.notifications.NotificationActionReceiver"
            android:exported="false" />
        <!-- После загрузки, перевода часов и смены зоны будильники строятся заново из базы (PLAN D8). -->
        <receiver
            android:name=".platform.notifications.BootAndTimeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- WorkManager собирает фоновые задачи графом (MedApp), а не своим инициализатором. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>